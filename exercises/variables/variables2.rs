// variables2.rs
// 执行 `rustlings hint variables2` 或在观察模式下使用 `hint` 子命令来获取提示。

// I AM NOT DONE

fn main() {
    let x;
    if x == 10 {
        println!("x 是 10！");
    } else {
        println!("x 不是 10！");
    }
}
