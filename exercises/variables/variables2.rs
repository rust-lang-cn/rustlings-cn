// variables2.rs
// 执行 `rustlings hint variables2` 或在观察模式下使用 `hint` 子命令来获取提示。

fn main() {
    let x: i32 = 10; /* 整数类型默认是i32 */
    if x == 10 {
        println!("x 是 10！");
    } else {
        println!("x 不是 10！");
    }
}
